apiVersion: v1
kind: Service
metadata:
  name: titanic-api-service
  namespace: titanic
  labels:
    app: titanic-api
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: titanic-api
